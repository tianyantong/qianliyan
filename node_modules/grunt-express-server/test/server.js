// Generated by CoffeeScript 1.7.1
(function() {
  "use strict";

  /*
  Test Server - CoffeeScript Edition
   */
  var app, log, start;

  app = require("./app.coffee");

  start = Date.now();

  log = function(message) {
    return console.log("[" + (Date.now() - start) + "] " + message);
  };

  log("Begin coffeescript-server.coffee");

  setTimeout((function() {
    return module.exports = app.listen(app.get("port"), function() {
      return log("Express server listening on port " + app.get("port"));
    });
  }), 50);

  setTimeout((function() {
    return log("250ms timeout");
  }), 250);

  log("End coffeescript-server.coffee");

}).call(this);
